---
phase: 04-social-sharing
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [
  "backend/app/api/daily.py",
  "backend/app/main.py",
  "backend/app/models/__init__.py",
  "backend/tests/test_daily_api.py"
]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Users can submit daily carbon entries via authenticated API"
    - "API returns streak data and contribution graph data"
    - "Daily entries are validated and constrained to one per day per user"
    - "Backend calculates carbon consciousness intensity for heatmap visualization"
  artifacts:
    - path: "backend/app/api/daily.py"
      provides: "Daily tracking API endpoints"
      contains: "router = APIRouter()"
    - path: "backend/tests/test_daily_api.py"
      provides: "Daily API test coverage"
      contains: "def test_create_daily_entry"
  key_links:
    - from: "backend/app/api/daily.py"
      to: "backend/app/auth/firebase_auth.py"
      via: "Function call"
      pattern: "verify_firebase_token"
    - from: "backend/app/api/daily.py"
      to: "backend/app/models/daily_entry.py"
      via: "SQLAlchemy ORM"
      pattern: "DailyEntry"
---

<objective>
Build the backend API for daily carbon tracking with Firebase authentication integration. This enables streak tracking and contribution graph data collection for the social features.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-social-sharing/04-CONTEXT.md
@.planning/phases/04-social-sharing/04-RESEARCH.md
@backend/app/api/ledger.py
@backend/app/models/daily_entry.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Daily Tracking API Endpoints</name>
  <read_first>
    backend/app/api/ledger.py
    backend/app/models/daily_entry.py
    backend/app/auth/firebase_auth.py
    backend/app/main.py
  </read_first>
  <action>
    Create backend/app/api/daily.py with FastAPI router for daily carbon tracking:
    
    Imports:
    - from datetime import date, datetime
    - from typing import List, Optional
    - from fastapi import APIRouter, Depends, HTTPException, Header
    - from sqlalchemy.ext.asyncio import AsyncSession
    - from sqlalchemy import select, func, extract
    - from pydantic import BaseModel, Field
    - from app.database import get_session
    - from app.auth.firebase_auth import verify_firebase_token
    - from app.models.daily_entry import DailyEntry
    - from app.models.user import User
    
    Pydantic models:
    - DailyEntryCreate: transport_mode, meals_count, energy_usage, carbon_consciousness (1-5 int)
    - DailyEntryResponse: id, date, transport_mode, meals_count, energy_usage, carbon_consciousness, created_at
    - ContributionData: date, intensity (0-4 scale for heatmap)
    - StreakResponse: current_streak, longest_streak, total_days, contributions: List[ContributionData]
    
    Endpoints:
    1. POST /daily - Create daily entry:
       - Verify Firebase token from Authorization header
       - Validate only one entry per day (handle IntegrityError)
       - Calculate carbon_consciousness from form inputs if not provided
       - Return DailyEntryResponse
    
    2. GET /daily/streak - Get user streak data:
       - Verify Firebase token from Authorization header
       - Query all daily entries for user, ordered by date
       - Calculate current streak (consecutive days ending today)
       - Calculate longest streak in history
       - Return contribution graph data (365 days, date + intensity)
       - Return StreakResponse
    
    3. GET /daily/entries - Get user's daily entries:
       - Verify Firebase token from Authorization header  
       - Query entries with optional date range filters
       - Return List[DailyEntryResponse]
    
    Router setup:
    - router = APIRouter(prefix="/api/daily", tags=["daily"])
  </action>
  <acceptance_criteria>
    - backend/app/api/daily.py contains "router = APIRouter(prefix=\"/api/daily\""
    - daily.py contains "async def create_daily_entry"
    - daily.py contains "async def get_streak_data"
    - daily.py contains "async def get_daily_entries"
    - daily.py contains "verify_firebase_token" import
    - daily.py contains "DailyEntryCreate" pydantic model
    - daily.py contains "StreakResponse" pydantic model
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -c "from app.api.daily import router; print(f'Router prefix: {router.prefix}')"</automated>
  </verify>
  <done>Daily tracking API endpoints created with Firebase auth integration and streak calculation logic.</done>
</task>

<task type="auto">
  <name>Task 2: Register Daily Router and Update Models Init</name>
  <read_first>
    backend/app/main.py
    backend/app/models/__init__.py
    backend/app/api/daily.py
  </read_first>
  <action>
    Register daily router in backend/app/main.py:
    - Import daily router: from app.api.daily import router as daily_router
    - Add app.include_router(daily_router) after existing router registrations
    
    Update backend/app/models/__init__.py to ensure all models are imported:
    - Add: from .user import User
    - Add: from .daily_entry import DailyEntry
    - Ensure existing imports remain: from .session import Session, Message, LedgerEntry
    - Ensure existing imports remain: from .snapshot import Snapshot
    
    This ensures SQLAlchemy can create all tables and relationships properly.
  </action>
  <acceptance_criteria>
    - backend/app/main.py contains "from app.api.daily import router as daily_router"
    - main.py contains "app.include_router(daily_router)"
    - backend/app/models/__init__.py contains "from .user import User"
    - models/__init__.py contains "from .daily_entry import DailyEntry"
    - models/__init__.py contains existing imports for Session, Message, LedgerEntry, Snapshot
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -c "from app.models import User, DailyEntry, Session, Snapshot; print('All models imported successfully')"</automated>
  </verify>
  <done>Daily router registered in FastAPI app and all models properly imported for SQLAlchemy.</done>
</task>

<task type="auto">
  <name>Task 3: Daily API Tests</name>
  <read_first>
    backend/tests/test_api_ledger.py
    backend/app/api/daily.py
    backend/tests/test_firebase_auth.py
  </read_first>
  <action>
    Create backend/tests/test_daily_api.py with comprehensive test coverage:
    
    Imports:
    - import pytest
    - import pytest_asyncio
    - from datetime import date, datetime, timedelta
    - from unittest.mock import patch, MagicMock
    - from httpx import AsyncClient
    - from app.main import app
    - from app.models.daily_entry import DailyEntry
    - from app.models.user import User
    - from app.database import get_session
    
    Test functions:
    1. test_create_daily_entry_success() - Mock verify_firebase_token, test POST /api/daily with valid data
    2. test_create_daily_entry_duplicate_date() - Test constraint violation for same date
    3. test_create_daily_entry_unauthorized() - Test without valid Firebase token
    4. test_get_streak_data_empty() - Test streak calculation with no entries
    5. test_get_streak_data_with_entries() - Test streak calculation with sample entries
    6. test_get_daily_entries() - Test GET /api/daily/entries endpoint
    7. test_contribution_graph_data() - Test 365-day contribution data format
    
    Setup:
    - Use AsyncClient for all HTTP tests
    - Mock verify_firebase_token to return "test_uid" in successful tests
    - Create test database fixtures similar to existing test patterns
    - Use @pytest.mark.asyncio for all async test functions
  </action>
  <acceptance_criteria>
    - backend/tests/test_daily_api.py contains "def test_create_daily_entry_success"
    - test file contains "def test_get_streak_data_with_entries"
    - test file contains "def test_create_daily_entry_unauthorized"  
    - test file contains "@pytest.mark.asyncio" decorators
    - test file contains "AsyncClient" import and usage
    - test file contains mock for "verify_firebase_token"
    - test file contains 7 test functions total
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest tests/test_daily_api.py -v</automated>
  </verify>
  <done>Daily API thoroughly tested with Firebase auth integration and streak calculation scenarios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Daily API | Firebase JWT tokens for user authentication |
| Daily API → Database | User-scoped daily entry operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Authorization | Daily entries | mitigate | Verify Firebase UID matches entry owner for all operations |
| T-04-05 | Tampering | Streak data | mitigate | Server-side streak calculation, no client input accepted |
| T-04-06 | Info Disclosure | User data | mitigate | Only return data for authenticated user's Firebase UID |
</threat_model>

<verification>
- Daily entry creation requires valid Firebase token
- Streak calculations are server-side and tamper-resistant
- API returns only user's own data based on Firebase UID
- Database constraints prevent duplicate entries per day
</verification>

<success_criteria>
Authenticated daily tracking API enables secure streak tracking and contribution graph data.
</success_criteria>

<output>
Create `.planning/phases/04-social-sharing/04-02-SUMMARY.md` when done
</output>