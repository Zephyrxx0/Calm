---
phase: 04-social-sharing
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified: [
  "frontend/src/components/broadsheet/NewspaperLayout.tsx",
  "frontend/src/app/edition/[sessionId]/page.tsx",
  "frontend/src/app/api/og/route.tsx",
  "backend/app/models/snapshot.py",
  "backend/app/api/snapshot.py",
  "frontend/tests/BroadsheetExport.test.tsx"
]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Edition page exports in true 11×17 broadsheet dimensions with multi-column layout"
    - "Social share cards (1200×630) display carbon stats with Calm branding"
    - "Public snapshot links include Firebase user association and streak data"
    - "Broadsheet layout renders properly across browsers with newspaper typography"
  artifacts:
    - path: "frontend/src/components/broadsheet/NewspaperLayout.tsx"
      provides: "True broadsheet newspaper layout"
      contains: "NewspaperLayout"
    - path: "frontend/src/app/api/og/route.tsx"
      provides: "Social share card generation"
      contains: "ImageResponse"
    - path: "backend/app/models/snapshot.py"
      provides: "Firebase UID-linked snapshots"
      contains: "firebase_uid"
  key_links:
    - from: "frontend/src/components/broadsheet/NewspaperLayout.tsx"
      to: "CSS Multi-column"
      via: "CSS properties"
      pattern: "columns: 4|column-gap"
    - from: "frontend/src/app/api/og/route.tsx"
      to: "next/og"
      via: "ImageResponse"
      pattern: "ImageResponse"
    - from: "backend/app/models/snapshot.py"
      to: "backend/app/models/user.py"
      via: "ForeignKey"
      pattern: "firebase_uid.*ForeignKey"
---

<objective>
Implement true newspaper broadsheet export layout and social sharing features with Firebase user integration. This completes the social features by enabling high-quality exports and shareable content.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-social-sharing/04-CONTEXT.md
@.planning/phases/04-social-sharing/04-UI-SPEC.md
@frontend/src/app/edition/[sessionId]/page.tsx
@backend/app/models/snapshot.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: True Broadsheet Layout Component</name>
  <read_first>
    frontend/src/app/edition/[sessionId]/page.tsx
    frontend/src/components/charts/OrganicBar.tsx
    .planning/phases/04-social-sharing/04-UI-SPEC.md
  </read_first>
  <action>
    Create frontend/src/components/broadsheet/NewspaperLayout.tsx with true 11"×17" dimensions (D-39, D-40):
    - Import useState, useEffect for print/export state management
    - Import html-to-image for PNG export capability
    - Broadsheet container with CSS:
      * @media print { width: 17in; height: 11in; columns: 4; column-gap: 0.5in; }
      * Font stack: serif headings (Times New Roman, Georgia), sans body text
      * Traditional newspaper masthead: "Calm Carbon Report" with decorative rules
      * Multi-column text flow with proper column breaks
    - Props interface: title, subtitle, footprint, categoryBreakdown, streakData, pullQuotes
    - Layout sections:
      1. Masthead with date and "Calm Carbon Report" title
      2. Headline section with user's total footprint prominently displayed
      3. Category breakdown in tabular format across columns
      4. Streak statistics and contribution graph summary
      5. Pull quotes and recommendations in sidebar boxes
    - Export functions:
      * exportAsPNG(): uses html-to-image with 2x pixelRatio
      * exportAsPDF(): uses html-to-image + jsPDF for multi-page support
    - Browser compatibility checks for CSS Multi-column support
  </action>
  <acceptance_criteria>
    - frontend/src/components/broadsheet/NewspaperLayout.tsx contains "NewspaperLayout"
    - NewspaperLayout.tsx contains "width: 17in; height: 11in"
    - NewspaperLayout.tsx contains "columns: 4" or "column-count: 4"
    - NewspaperLayout.tsx contains "html-to-image" import
    - NewspaperLayout.tsx contains "exportAsPNG" function
    - NewspaperLayout.tsx contains "Calm Carbon Report" masthead text
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "17in\|11in\|columns.*4\|exportAsPNG" src/components/broadsheet/NewspaperLayout.tsx</automated>
  </verify>
  <done>True broadsheet layout component with newspaper dimensions and export capabilities.</done>
</task>

<task type="auto">
  <name>Task 2: Enhanced Edition Page with Broadsheet Export</name>
  <read_first>
    frontend/src/app/edition/[sessionId]/page.tsx
    frontend/src/components/broadsheet/NewspaperLayout.tsx
    frontend/src/components/daily/ContributionGraph.tsx
  </read_first>
  <action>
    Update frontend/src/app/edition/[sessionId]/page.tsx to integrate broadsheet layout:
    - Import NewspaperLayout component
    - Add export mode toggle: "Standard View" vs "Broadsheet Preview"
    - In broadsheet mode, render NewspaperLayout with session data:
      * Pass footprint calculation from existing edition API
      * Pass category breakdown data
      * Include streak data if user is authenticated (useAuth check)
      * Pass pull quotes from existing interview data
    - Export controls:
      * "Download Broadsheet (PNG)" button → calls NewspaperLayout.exportAsPNG()
      * "Download Broadsheet (PDF)" button → calls NewspaperLayout.exportAsPDF()
      * "Share Social Card" button → creates snapshot and generates OG image URL
    - Responsive behavior:
      * Mobile: stack export controls vertically
      * Desktop: horizontal layout with preview pane
    - Loading states during export operations
    - Error handling for export failures (unsupported browser, insufficient data)
  </action>
  <acceptance_criteria>
    - frontend/src/app/edition/[sessionId]/page.tsx imports "NewspaperLayout"
    - edition page contains "Broadsheet Preview" or "broadsheet mode" text
    - edition page contains "Download Broadsheet (PNG)" button
    - edition page contains "Download Broadsheet (PDF)" button
    - edition page contains "Share Social Card" button
    - edition page calls "exportAsPNG" or "exportAsPDF" methods
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "NewspaperLayout\|Broadsheet.*PNG\|exportAsPNG" src/app/edition/[sessionId]/page.tsx</automated>
  </verify>
  <done>Edition page integrates broadsheet export with proper controls and Firebase user data.</done>
</task>

<task type="auto">
  <name>Task 3: Social Share Card Generation</name>
  <read_first>
    frontend/src/app/api/og/route.tsx
    backend/app/models/snapshot.py
    .planning/phases/04-social-sharing/04-UI-SPEC.md
  </read_first>
  <action>
    Create frontend/src/app/api/og/route.tsx for universal social cards (D-42, D-43):
    - Import ImageResponse from 'next/og'
    - GET endpoint accepting query params: snapshotId, totalFootprint, streakDays, topCategory
    - Social card design (1200×630 dimensions):
      * Background: warm off-white (#f4f3ef) with subtle texture
      * Calm logo/branding in top-left corner
      * Large display of carbon footprint (e.g., "4.2 tonnes CO₂/year")
      * Streak information if available (e.g., "🔥 12 day streak")
      * Top carbon category highlighted
      * Soft accent colors (#c2856b) for CTAs and highlights
      * Footer: "Track your carbon footprint at calm.app"
    - Typography: Geist font family (consistent with UI-SPEC)
    - Fallback for missing data: generic "Start your carbon journey" message
    - Cache headers for social platform optimization
    
    Update backend/app/models/snapshot.py to include Firebase user association:
    - Add firebase_uid field: Mapped[str | None] = mapped_column(String(128), ForeignKey("users.firebase_uid"), nullable=True)
    - This links snapshots to authenticated users while maintaining anonymous session support
    - Update existing snapshots creation in API to include Firebase UID when available
  </action>
  <acceptance_criteria>
    - frontend/src/app/api/og/route.tsx contains "ImageResponse"
    - og/route.tsx contains "1200" and "630" dimensions
    - og/route.tsx contains "tonnes CO₂" or carbon footprint display
    - og/route.tsx contains "#f4f3ef" or "#c2856b" color values
    - backend/app/models/snapshot.py contains "firebase_uid.*ForeignKey"
    - snapshot.py contains "users.firebase_uid" reference
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "ImageResponse\|1200.*630" src/app/api/og/route.tsx && cd ../backend && grep -c "firebase_uid.*ForeignKey" app/models/snapshot.py</automated>
  </verify>
  <done>Social share cards generated with user data and Firebase user-linked snapshots created.</done>
</task>

<task type="auto">
  <name>Task 4: Update Snapshot API for Firebase Users</name>
  <read_first>
    backend/app/api/snapshot.py
    backend/app/models/snapshot.py
    backend/app/auth/firebase_auth.py
  </read_first>
  <action>
    Update backend/app/api/snapshot.py to handle Firebase authenticated users:
    - Import verify_firebase_token from app.auth.firebase_auth
    - Modify POST /api/snapshot endpoint:
      * Accept optional Authorization header for Firebase token
      * If token provided, verify it and extract Firebase UID
      * Include firebase_uid in snapshot creation
      * Include user's daily streak data in snapshot payload if authenticated
    - Modify GET /api/snapshot/{snapshot_id} endpoint:
      * Return enhanced data for Firebase user snapshots (streak info, user stats)
      * Maintain compatibility with anonymous session snapshots
    - Add GET /api/snapshot/user/{firebase_uid} endpoint:
      * List all snapshots for a specific Firebase user
      * Verify requesting user matches target UID or is admin
      * Return paginated list of user's snapshots with metadata
    - Update snapshot payload schema to include:
      * streak_data: {current_streak, longest_streak, total_days}
      * user_metadata: {display_name, created_at}
      * enhanced_stats: category breakdown with streak context
  </action>
  <acceptance_criteria>
    - backend/app/api/snapshot.py imports "verify_firebase_token"
    - snapshot.py POST endpoint contains "Authorization header" handling
    - snapshot.py contains "firebase_uid" in snapshot creation
    - snapshot.py contains "GET /api/snapshot/user/" endpoint
    - snapshot.py contains "streak_data" in payload
    - snapshot.py contains enhanced user snapshot handling
  </acceptance_criteria>
  <verify>
    <automated>cd backend && grep -c "verify_firebase_token\|firebase_uid\|streak_data" app/api/snapshot.py</automated>
  </verify>
  <done>Snapshot API enhanced with Firebase user integration and streak data inclusion.</done>
</task>

<task type="auto">
  <name>Task 5: Broadsheet Export Tests</name>
  <read_first>
    frontend/tests/Summary.test.tsx
    frontend/src/components/broadsheet/NewspaperLayout.tsx
    frontend/src/app/edition/[sessionId]/page.tsx
  </read_first>
  <action>
    Create frontend/tests/BroadsheetExport.test.tsx with comprehensive coverage:
    - Test NewspaperLayout component rendering:
      * Renders with proper 11"×17" CSS dimensions in print media
      * Displays masthead "Calm Carbon Report" correctly
      * Shows footprint data in newspaper format
      * Applies multi-column layout styling
    - Test export functionality:
      * Mock html-to-image library for PNG export testing
      * Test exportAsPNG method calls html-to-image correctly
      * Test exportAsPDF method integration
      * Test export loading states and error handling
    - Test enhanced Edition page:
      * Renders broadsheet preview toggle
      * Shows export buttons when in broadsheet mode
      * Integrates with Firebase user data when authenticated
      * Handles unauthenticated users gracefully
    - Test social share integration:
      * Mock OG image URL generation
      * Test social card data preparation
      * Test snapshot creation with Firebase UID
    
    Use vitest, @testing-library/react, and MSW for API mocking.
    Use it.todo() for complex browser compatibility tests.
  </action>
  <acceptance_criteria>
    - frontend/tests/BroadsheetExport.test.tsx contains "test.*NewspaperLayout"
    - BroadsheetExport.test.tsx contains "test.*exportAsPNG"
    - BroadsheetExport.test.tsx contains "test.*broadsheet.*mode"
    - BroadsheetExport.test.tsx contains "html-to-image" mock
    - BroadsheetExport.test.tsx contains "11in.*17in" or dimension tests
    - BroadsheetExport.test.tsx contains Firebase user integration tests
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run tests/BroadsheetExport.test.tsx</automated>
  </verify>
  <done>Comprehensive tests cover broadsheet export functionality and social sharing integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → OG API | Social platform scrapers fetching images |
| Frontend Export | Browser-based image generation |
| Firebase User Snapshots | User-linked permanent data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Denial of Service | OG Image Generation | mitigate | Cache OG images, rate limit by IP |
| T-04-11 | Info Disclosure | User Snapshots | mitigate | Verify Firebase UID ownership on snapshot access |
| T-04-12 | Tampering | Export Data | mitigate | Client-side export only, no sensitive data in DOM |
</threat_model>

<verification>
- Broadsheet exports render in true 11"×17" dimensions
- Social share cards display correctly across platforms
- Firebase user data integrates securely with snapshots
- Export functionality works across supported browsers
</verification>

<success_criteria>
Complete social sharing infrastructure with newspaper-quality exports and universal social cards.
</success_criteria>

<output>
Create `.planning/phases/04-social-sharing/04-04-SUMMARY.md` when done
</output>