---
phase: 04-social-sharing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "backend/requirements.txt",
  "backend/app/models/user.py", 
  "backend/app/models/daily_entry.py",
  "backend/app/auth/firebase_auth.py",
  "frontend/package.json",
  "frontend/src/lib/firebase.ts",
  "frontend/src/contexts/AuthContext.tsx",
  "backend/tests/test_firebase_auth.py"
]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Backend verifies Firebase tokens using Firebase Admin SDK"
    - "Frontend manages auth state via AuthContext with Firebase client SDK"
    - "Daily entries are linked to Firebase UIDs in PostgreSQL"
    - "User model stores minimal Firebase profile data"
  artifacts:
    - path: "backend/app/auth/firebase_auth.py"
      provides: "Firebase Admin SDK token verification"
      contains: "def verify_firebase_token"
    - path: "frontend/src/contexts/AuthContext.tsx"
      provides: "Firebase auth state management"
      contains: "AuthContext"
    - path: "backend/app/models/daily_entry.py"
      provides: "Daily tracking database model"
      contains: "class DailyEntry"
  key_links:
    - from: "frontend/src/contexts/AuthContext.tsx"
      to: "firebase/auth"
      via: "Firebase client SDK"
      pattern: "signInWithEmailAndPassword"
    - from: "backend/app/auth/firebase_auth.py"
      to: "firebase_admin.auth"
      via: "Firebase Admin SDK"
      pattern: "verify_id_token"
---

<objective>
Implement Firebase Auth integration for user accounts and create database models for daily carbon tracking. This breaks the "no accounts" principle from Phases 1-3 to enable cross-device sync for streak tracking.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-social-sharing/04-CONTEXT.md
@.planning/phases/04-social-sharing/04-RESEARCH.md
@backend/app/models/session.py
@backend/app/models/snapshot.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Firebase Admin SDK Setup</name>
  <read_first>
    backend/requirements.txt
    backend/app/models/session.py
  </read_first>
  <action>
    Add firebase-admin>=6.5.0 to backend/requirements.txt.
    
    Create backend/app/auth/firebase_auth.py with Firebase Admin SDK integration:
    - Import firebase_admin and firebase_admin.auth
    - Initialize Firebase Admin app with credentials from environment
    - Implement async def verify_firebase_token(token: str) -> str function that:
      * Calls firebase_admin.auth.verify_id_token(token)
      * Returns decoded_token.uid on success
      * Raises HTTPException(401, "Invalid token") on verification failure
    - Add error handling for ExpiredIdTokenError and InvalidIdTokenError
    
    Create backend/app/models/user.py with minimal User model:
    - Table name: "users"  
    - firebase_uid: Mapped[str] primary key (String(128))
    - email: Mapped[str] (String(255), nullable=True)
    - display_name: Mapped[str] (String(100), nullable=True)  
    - created_at: Mapped[datetime] (DateTime, default=func.now())
    
    Create backend/app/models/daily_entry.py:
    - Table name: "daily_entries"
    - id: Mapped[int] primary key
    - firebase_uid: Mapped[str] (String(128), ForeignKey("users.firebase_uid"))
    - date: Mapped[date] (Date, nullable=False)
    - transport_mode: Mapped[str] (String(50), nullable=True) # "walk", "bike", "car", "transit"
    - meals_count: Mapped[int] (nullable=True) # 1-4
    - energy_usage: Mapped[str] (String(50), nullable=True) # "low", "medium", "high"
    - carbon_consciousness: Mapped[int] (nullable=False) # 1-5 scale for heatmap intensity
    - created_at: Mapped[datetime] (DateTime, default=func.now())
    - UniqueConstraint('firebase_uid', 'date', name='one_entry_per_day')
  </action>
  <acceptance_criteria>
    - backend/requirements.txt contains "firebase-admin>=6.5.0"
    - backend/app/auth/firebase_auth.py contains "def verify_firebase_token("
    - backend/app/models/user.py contains "class User(Base):"
    - backend/app/models/daily_entry.py contains "class DailyEntry(Base):"
    - daily_entry.py contains "UniqueConstraint('firebase_uid', 'date'"
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -c "from app.auth.firebase_auth import verify_firebase_token; from app.models.user import User; from app.models.daily_entry import DailyEntry; print('Models imported successfully')"</automated>
  </verify>
  <done>Firebase Admin SDK configured and database models created with proper constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend Firebase Client Setup</name>
  <read_first>
    frontend/package.json
    frontend/src/app/layout.tsx
  </read_first>
  <action>
    Add firebase@10.13.1, react-calendar-heatmap@1.9.0, date-fns@2.30.0 to frontend/package.json using pnpm.
    
    Create frontend/src/lib/firebase.ts:
    - Import { initializeApp } from 'firebase/app'
    - Import { getAuth, connectAuthEmulator } from 'firebase/auth'
    - Export firebaseConfig object with placeholders:
      * apiKey: process.env.NEXT_PUBLIC_FIREBASE_API_KEY || ""
      * authDomain: process.env.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN || ""
      * projectId: process.env.NEXT_PUBLIC_FIREBASE_PROJECT_ID || ""
    - Export const app = initializeApp(firebaseConfig)
    - Export const auth = getAuth(app)
    - Add connectAuthEmulator for dev environment if not already connected
    
    Create frontend/src/contexts/AuthContext.tsx:
    - Import createContext, useContext, useEffect, useState from 'react'
    - Import { User, onAuthStateChanged, signInWithEmailAndPassword, createUserWithEmailAndPassword, signOut } from 'firebase/auth'
    - Import { auth } from '@/lib/firebase'
    - Define AuthContextType interface with user: User | null, loading: boolean, signIn, signUp, logOut functions
    - Create AuthContext with createContext<AuthContextType>
    - Export useAuth hook that returns useContext(AuthContext)
    - Export AuthProvider component that:
      * Manages user state with useState<User | null>
      * Manages loading state with useState<boolean>(true)  
      * Uses onAuthStateChanged in useEffect to track auth state
      * Provides signIn, signUp, logOut functions
      * Sets loading to false after initial auth state check
  </action>
  <acceptance_criteria>
    - frontend/package.json contains "firebase": "10.13.1"
    - frontend/package.json contains "react-calendar-heatmap": "1.9.0"
    - frontend/package.json contains "date-fns": "2.30.0"
    - frontend/src/lib/firebase.ts contains "export const auth ="
    - frontend/src/contexts/AuthContext.tsx contains "export const useAuth ="
    - AuthContext.tsx contains "signInWithEmailAndPassword"
    - AuthContext.tsx contains "onAuthStateChanged"
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm list firebase react-calendar-heatmap date-fns</automated>
  </verify>
  <done>Firebase client SDK configured with auth context provider for state management.</done>
</task>

<task type="auto">
  <name>Task 3: Backend Firebase Tests</name>
  <read_first>
    backend/tests/test_database.py
    backend/app/auth/firebase_auth.py
  </read_first>
  <action>
    Create backend/tests/test_firebase_auth.py:
    - Import pytest, pytest_asyncio, unittest.mock
    - Import HTTPException from fastapi
    - Import firebase_admin.auth exceptions (InvalidIdTokenError, ExpiredIdTokenError)
    - Import verify_firebase_token from app.auth.firebase_auth
    
    Add 4 test functions:
    1. test_verify_valid_token() - Mock firebase_admin.auth.verify_id_token to return {"uid": "test_uid"}, assert result equals "test_uid"
    2. test_verify_invalid_token() - Mock to raise InvalidIdTokenError, assert HTTPException(401) is raised  
    3. test_verify_expired_token() - Mock to raise ExpiredIdTokenError, assert HTTPException(401) is raised
    4. test_verify_malformed_token() - Mock to raise ValueError, assert HTTPException(401) is raised
    
    Use @pytest.mark.asyncio decorator for async tests.
    Use unittest.mock.patch("firebase_admin.auth.verify_id_token") for mocking.
    Assert exact error messages match "Invalid token".
  </action>
  <acceptance_criteria>
    - backend/tests/test_firebase_auth.py contains "def test_verify_valid_token"
    - test file contains "def test_verify_invalid_token" 
    - test file contains "@pytest.mark.asyncio"
    - test file contains "unittest.mock.patch"
    - test file contains 4 test functions total
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest tests/test_firebase_auth.py -v</automated>
  </verify>
  <done>Firebase auth verification thoroughly tested with mocked Firebase Admin SDK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Firebase Auth | User credentials via Firebase client SDK |
| Backend API → Firebase Admin | Token verification via Firebase Admin SDK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | Firebase tokens | mitigate | Verify all tokens server-side with Firebase Admin SDK |
| T-04-02 | Tampering | Daily entry data | mitigate | Validate Firebase UID ownership of entries |
| T-04-03 | Info Disclosure | User profiles | mitigate | Store minimal profile data (email, display name only) |
</threat_model>

<verification>
- Firebase Admin SDK successfully verifies tokens
- AuthContext properly manages auth state changes  
- Database models enforce user ownership via Firebase UIDs
- Tests cover token verification edge cases
</verification>

<success_criteria>
Firebase Auth fully integrated with minimal user accounts for cross-device sync.
</success_criteria>

<output>
Create `.planning/phases/04-social-sharing/04-01-SUMMARY.md` when done
</output>