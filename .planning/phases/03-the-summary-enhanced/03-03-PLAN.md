---
phase: 03-the-summary-enhanced
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [frontend/src/app/share/[snapshotId]/page.tsx, frontend/src/app/api/og/route.tsx, frontend/src/app/edition/[sessionId]/page.tsx, package.json]
autonomous: true
requirements: [SHARING]

must_haves:
  truths:
    - "Users can generate a public, shareable link to their carbon report"
    - "Social media platforms display a custom stats card when the link is shared"
    - "Users can download a high-quality PDF/Image version of the report"
  artifacts:
    - path: "frontend/src/app/share/[snapshotId]/page.tsx"
      provides: "Public read-only report view"
    - path: "frontend/src/app/api/og/route.tsx"
      provides: "Dynamic social share cards"
---

<objective>
Enable multi-format sharing for the enhanced carbon reports. This includes public web links with dynamic social preview cards and high-quality local downloads (Image/PDF).
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-the-summary-enhanced/03-CONTEXT.md
@.planning/phases/03-the-summary-enhanced/03-RESEARCH.md
@frontend/src/app/edition/[sessionId]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Public Share Page</name>
  <files>frontend/src/app/share/[snapshotId]/page.tsx</files>
  <action>
    Create the read-only share page at `frontend/src/app/share/[snapshotId]/page.tsx`.
    - Fetch snapshot data from `GET /api/snapshot/{snapshotId}`.
    - Reuse the components from `SummaryPage` (charts, layout) but in a read-only mode (no "Back" button to interview, no "Edit" capabilities).
    - Add a "Create Your Own Interview" call-to-action for visitors.
  </action>
  <verify>
    <automated>npm run test -- frontend/tests/SharePage.test.tsx</automated>
  </verify>
  <done>Public share page renders correctly from snapshot data.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Social Share Cards</name>
  <files>frontend/src/app/api/og/route.tsx, frontend/src/app/share/[snapshotId]/page.tsx</files>
  <action>
    Implement `frontend/src/app/api/og/route.tsx` using `next/og` (Satori).
    - The endpoint should accept `snapshotId` (or stats via query params) and render a beautiful Social Card with the user's total tonnage and category breakdown.
    - Use the brand fonts and colors (sage, warm off-white).
    - Add Open Graph (OG) and Twitter meta tags to both the `Edition` and `Share` pages pointing to this endpoint.
  </action>
  <verify>
    <automated>curl -I http://localhost:3000/api/og?total=5.2</automated>
  </verify>
  <done>Dynamic social cards are generated and linked via meta tags.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Hardened PDF/Image Export</name>
  <files>frontend/src/app/edition/[sessionId]/page.tsx, package.json</files>
  <action>
    Install `jspdf` via pnpm.
    Upgrade the "Download" buttons on the Edition page:
    - **Image:** Refine `html-to-image` settings (embed fonts, set higher quality).
    - **PDF:** Implement PDF export using `html-to-image` to capture the DOM and `jspdf` to embed the resulting image into a multi-page or high-res A4 PDF.
    - Add a "Share Link" button that triggers the `POST /api/snapshot` call and displays the resulting public URL in a copyable dialog.
  </action>
  <verify>
    <checkpoint:human-verify gate="blocking">
      <what-built>Download functionality and Public Link generation</what-built>
      <how-to-verify>
        1. Click "Download Image" -> verify .png file is saved.
        2. Click "Download PDF" -> verify .pdf file is saved.
        3. Click "Share Link" -> copy URL and open in Incognito -> verify same report is visible.
      </how-to-verify>
      <resume-signal>approved</resume-signal>
    </checkpoint:human-verify>
  </verify>
  <done>Reports can be shared via URL or downloaded as Image/PDF.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Social Platform → OG API | Platforms fetching the dynamic image |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Denial of Service | OG Image Generation | mitigate | OG generation is resource-intensive; ensure the endpoint is cached (Vercel Edge caching). |
</threat_model>

<verification>
Test sharing on a platform (or use Opengraph.xyz):
1. Input a public share URL.
2. Verify the preview card displays the correct carbon stats and "Calm" branding.
</verification>

<success_criteria>
- Snapshot retrieval and rendering complete.
- Dynamic OG image generation active.
- Image and PDF export functionality hardened.
</success_criteria>

<output>
Create `.planning/phases/03-the-summary-enhanced/03-03-SUMMARY.md` when done
</output>
