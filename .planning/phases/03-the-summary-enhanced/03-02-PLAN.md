---
phase: 03-the-summary-enhanced
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [frontend/src/components/charts/OrganicBar.tsx, backend/app/services/insights.py, backend/app/api/edition.py, frontend/src/app/edition/[sessionId]/page.tsx]
autonomous: true
requirements: [DETAIL-SUMMARY, VISUALIZATIONS, BENCHMARKS]

must_haves:
  truths:
    - "Carbon breakdown is rendered using hand-drawn style charts"
    - "AI-generated insights provide a personalized conversational summary of the footprint"
    - "Users can toggle between Global and National benchmark comparisons"
  artifacts:
    - path: "frontend/src/components/charts/OrganicBar.tsx"
      provides: "Organic-styled Recharts components"
    - path: "backend/app/services/insights.py"
      provides: "AI-flavored summary logic"
---

<objective>
Upgrade the summary page with organic data visualizations and AI-powered insights. This plan focuses on the "look and feel" of the data and the depth of the analysis.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-the-summary-enhanced/03-CONTEXT.md
@.planning/phases/03-the-summary-enhanced/03-RESEARCH.md
@frontend/src/components/OrganicDoodles.tsx
@backend/app/services/ai_coach.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Organic Charting Components</name>
  <files>frontend/src/components/charts/OrganicBar.tsx</files>
  <action>
    Implement `OrganicBar` and `OrganicBarChart` components using `recharts`.
    - Use the `shape` prop in `Bar` to render a custom SVG path with "organic" (slightly wobbly/hand-drawn) edges (see Pattern 1 in RESEARCH.md).
    - Use colors from `UI-SPEC.md` (sage/muted green, warm grays).
    - Integrate `OrganicDoodles.tsx` elements for chart annotations or "flavor".
  </action>
  <verify>
    <automated>npm run test -- frontend/tests/OrganicBar.test.tsx</automated>
  </verify>
  <done>Charts render with an organic, calm aesthetic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AI Insights Engine</name>
  <files>backend/app/services/insights.py, backend/app/api/edition.py</files>
  <action>
    Create `backend/app/services/insights.py`.
    Implement `InsightsService` that uses `Gemini 1.5 Flash` to:
    - Summarize the user's carbon footprint in 2-3 "calm journalist" sentences.
    - Provide 3 actionable, category-level recommendations (e.g., "Since your travel footprint is high, consider...").
    - Use `SYSTEM_PROMPT` style from `ai_coach.py` for consistency.

    Update `backend/app/api/edition.py` to:
    - Call `InsightsService.get_insights()`.
    - Call `BenchmarkService.get_benchmarks()`.
    - Include these in the returned JSON payload.
  </action>
  <verify>
    <automated>pytest backend/tests/test_api_edition.py</automated>
  </verify>
  <done>Edition API returns personalized insights and comparative benchmarks.</done>
</task>

<task type="auto">
  <name>Task 3: Update Edition UI</name>
  <files>frontend/src/app/edition/[sessionId]/page.tsx</files>
  <action>
    Overhaul the `SummaryPage` UI:
    - Replace standard progress bars with `OrganicBarChart`.
    - Add a "Benchmarking" section with a toggle between "Global Average" and "National Average" (using data from API).
    - Display the AI-generated "Journalist's Insight" and "Top 3 Actions".
    - Apply consistent spacing and typography from `UI-SPEC.md`.
  </action>
  <verify>
    <automated>npm run test -- frontend/tests/Summary.test.tsx</automated>
  </verify>
  <done>Edition page displays charts, benchmarks, and AI insights beautifully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Backend → Gemini API | Sending user-derived data for summarization |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | Insights Service | mitigate | Do not send PII to Gemini; only send anonymous footprint stats and category labels. |
</threat_model>

<verification>
Check visual layout on `/edition/[sessionId]`:
1. Verify Recharts bars have organic shapes (not straight lines).
2. Toggle benchmarks and observe chart scale/comparison line changing.
3. Read the "Journalist's Insight" and ensure it matches the tone of the interview.
</verification>

<success_criteria>
- Recharts integrated with custom organic shapes.
- Gemini-powered insights active and integrated.
- Benchmark comparison toggle functional.
</success_criteria>

<output>
Create `.planning/phases/03-the-summary-enhanced/03-02-SUMMARY.md` when done
</output>
