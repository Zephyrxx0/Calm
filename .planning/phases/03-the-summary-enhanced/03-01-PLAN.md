---
phase: 03-the-summary-enhanced
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/models/snapshot.py, backend/app/services/benchmarks.py, backend/app/api/snapshot.py, backend/app/main.py]
autonomous: true
requirements: [BENCHMARKS, SHARING]

must_haves:
  truths:
    - "Snapshots can be saved to the database and retrieved by a UUID"
    - "The system provides global and national average carbon footprint data for comparison"
  artifacts:
    - path: "backend/app/models/snapshot.py"
      provides: "Persistence for shared reports"
    - path: "backend/app/services/benchmarks.py"
      provides: "Comparison data constants and logic"
  key_links:
    - from: "backend/app/api/snapshot.py"
      to: "backend/app/models/snapshot.py"
      via: "SQLAlchemy"
---

<objective>
Implement the backend infrastructure for shared reports (snapshots) and comparative benchmarks. This foundation allows users to save a static version of their report and see how they compare to national/global averages.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-the-summary-enhanced/03-CONTEXT.md
@.planning/phases/03-the-summary-enhanced/03-RESEARCH.md
@backend/app/models/session.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Snapshot Database Model</name>
  <files>backend/app/models/snapshot.py</files>
  <action>
    Create a new SQLAlchemy model `Snapshot` in `backend/app/models/snapshot.py`.
    Properties:
    - `id`: UUID (Primary Key, default=uuid4)
    - `session_id`: UUID (Foreign Key to sessions.id, nullable=True as snapshots might persist after sessions are deleted)
    - `payload`: JSONB (to store the entire edition data: footprint, messages, quotes, insights)
    - `created_at`: DateTime (default=func.now())

    Ensure `Base` is imported from `app.database`.
    Register the model in `backend/app/models/__init__.py`.
  </action>
  <verify>
    <automated>pytest backend/tests/test_database.py</automated>
  </verify>
  <done>Snapshot model is defined and migrations (if any/manual) are ready.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Benchmark Service</name>
  <files>backend/app/services/benchmarks.py</files>
  <action>
    Create `backend/app/services/benchmarks.py`.
    Implement `BenchmarkService` with a method `get_benchmarks(country: str = "Global")`.
    Include the following hardcoded data (from RESEARCH.md):
    - Global: 4.7
    - US: 14.5
    - UK: 4.25
    - EU: 6.3
    - India: 1.9
    Returns: `{"global": 4.7, "national": float (based on country), "label": str}`.
  </action>
  <verify>
    <automated>python3 -c "from app.services.benchmarks import BenchmarkService; print(BenchmarkService().get_benchmarks('UK'))"</automated>
  </verify>
  <done>Benchmark service returns correct comparison values.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Snapshot API</name>
  <files>backend/app/api/snapshot.py, backend/app/main.py</files>
  <action>
    Create `backend/app/api/snapshot.py` with FastAPI router.
    Endpoints:
    - `POST /snapshot`: Accepts a session_id. Fetches session data, generates the edition payload (using logic from `edition.py`), saves it to `Snapshots` table, and returns the snapshot `id`.
    - `GET /snapshot/{snapshot_id}`: Retrieves the stored payload by UUID.

    Register the router in `backend/app/main.py` under `/api`.
  </action>
  <verify>
    <automated>pytest backend/tests/test_api_endpoints.py</automated>
  </verify>
  <done>Endpoints for creating and retrieving snapshots are functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Public snapshot retrieval by UUID |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | GET /snapshot/{id} | mitigate | Use v4 UUIDs to prevent ID enumeration. |
| T-03-02 | Tampering | POST /snapshot | mitigate | Validate session existence before allowing snapshot creation. |
</threat_model>

<verification>
Verify snapshot creation via cURL:
1. Complete an interview to get a session_id.
2. `curl -X POST http://localhost:8000/api/snapshot?session_id=...` -> returns UUID.
3. `curl http://localhost:8000/api/snapshot/{uuid}` -> returns JSON payload.
</verification>

<success_criteria>
- Snapshot model is in the database.
- Benchmarks are available via service.
- API can save and load static report snapshots.
</success_criteria>

<output>
Create `.planning/phases/03-the-summary-enhanced/03-01-SUMMARY.md` when done
</output>
