---
phase: 04.1-edition-edits
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/layout.tsx
  - frontend/src/components/broadsheet/NewspaperLayout.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Masthead displays 'The Daily Calm' in Playfair Display Bold uppercase"
    - "Newspaper body text renders in PT Serif"
    - "Playfair Display and PT Serif fonts load via next/font/google with display: 'block'"
    - "Masthead subtitle 'Your Carbon Story, in Print' appears on edition page (conditional on space)"
  artifacts:
    - path: "frontend/src/app/layout.tsx"
      provides: "Playfair Display + PT Serif font declarations"
      contains: "Playfair_Display"
      contains: "PT_Serif"
      contains: "display: 'block'"
    - path: "frontend/src/components/broadsheet/NewspaperLayout.tsx"
      provides: "Updated masthead with 'The Daily Calm' and newspaper fonts"
      contains: "The Daily Calm"
  key_links:
    - from: "layout.tsx font CSS variables"
      to: "NewspaperLayout.tsx CSS font-family declarations"
      via: "var(--font-newspaper-headline) and var(--font-newspaper-body)"
      pattern: "var\\(--font-newspaper-(headline|body)\\)"
---

<objective>
Load Playfair Display (headlines) and PT Serif (body) via next/font/google with `display: 'block'` for export reliability, expose them as CSS variables, and update the NewspaperLayout masthead from "Calm Carbon Report" to "The Daily Calm" with its new subtitle. This is the typographic foundation that Plans 02 and 03 build on.

Purpose: Establish authentic broadsheet typography before visual effects are applied. Without this, subsequent plans would reference font variables that do not exist.
Output: Two Google Fonts loaded and available to NewspaperLayout; masthead text updated; font-family declarations in NewspaperLayout switched from system serifs to the new CSS variables.
</objective>

<execution_context>
@/home/zeph/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/zeph/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.1-edition-edits/04.1-CONTEXT.md
@.planning/phases/04.1-edition-edits/04.1-RESEARCH.md
@.planning/phases/04.1-edition-edits/04.1-UI-SPEC.md
@frontend/src/app/layout.tsx
@frontend/src/components/broadsheet/NewspaperLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Playfair Display and PT Serif fonts to root layout</name>
  <files>frontend/src/app/layout.tsx</files>
  <read_first>
    - frontend/src/app/layout.tsx — current font declarations (Instrument_Serif, Geist, Geist_Mono), CSS variable pattern, html className assembly
    - .planning/phases/04.1-edition-edits/04.1-UI-SPEC.md — Section "Typography — Newspaper Context", Four-Tier Size System table, Declared Font Weights table
  </read_first>
  <action>
    In frontend/src/app/layout.tsx, add two new font declarations after the existing three:

    1. Import Playfair_Display and PT_Serif from 'next/font/google' — add them to the existing import statement on line 2 alongside Instrument_Serif, Geist, Geist_Mono.

    2. Declare `playfairDisplay` constant:
       - Playfair_Display({ subsets: ['latin'], weight: '700', variable: '--font-newspaper-headline', display: 'block' })
       - Pattern: follow the existing `instrumentSerif` declaration style but with `display: 'block'` (not 'swap') — this is critical per D-53 for html-to-image export reliability.

    3. Declare `ptSerif` constant:
       - PT_Serif({ subsets: ['latin'], weight: ['400', '700'], variable: '--font-newspaper-body', display: 'block' })
       - Two weights: 400 Regular for body text, 700 Bold for table headers per UI-SPEC Declared Font Weights table.

    4. Append `${playfairDisplay.variable} ${ptSerif.variable}` to the html `className` on line 41 (inside the template literal with the existing font variables).

    Do NOT modify any existing font declarations (Instrument_Serif, Geist, Geist_Mono). Do NOT add any other CSS or style changes. Do NOT remove any existing code.
  </action>
  <verify>
    <automated>grep -c "Playfair_Display" frontend/src/app/layout.tsx | xargs -I{} sh -c '[ {} -ge 2 ]'</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx imports Playfair_Display and PT_Serif from 'next/font/google'
    - playfairDisplay constant uses variable: '--font-newspaper-headline', display: 'block', weight: '700'
    - ptSerif constant uses variable: '--font-newspaper-body', display: 'block', weight: ['400', '700']
    - html className includes `${playfairDisplay.variable} ${ptSerif.variable}` in the template literal
    - Existing font declarations (Instrument_Serif, Geist, Geist_Mono) are unmodified
    - TypeScript compilation: `npx tsc --noEmit` passes for layout.tsx
  </acceptance_criteria>
  <done>Playfair Display Bold (700) and PT Serif Regular+Bold (400, 700) are loaded via next/font/google with display: 'block', exposed as --font-newspaper-headline and --font-newspaper-body CSS variables on the html element.</done>
</task>

<task type="auto">
  <name>Task 2: Update NewspaperLayout masthead text and font-family declarations</name>
  <files>frontend/src/components/broadsheet/NewspaperLayout.tsx</files>
  <read_first>
    - frontend/src/components/broadsheet/NewspaperLayout.tsx — current masthead title (line 226: "Calm Carbon Report"), masthead styles (lines 424-456), headline styles (lines 458-510), section label styles (lines 513-524), category table font (line 537), quote styles (lines 606-629), footer styles (lines 640-652), streak styles (lines 570-596)
    - .planning/phases/04.1-edition-edits/04.1-UI-SPEC.md — Section "Typography — Newspaper Context" (Four-Tier Size System, Declared Font Weights), Section "Copywriting Contract" (masthead title "The Daily Calm", subtitle "Your Carbon Story, in Print"), Section "Color — Newspaper Context"
  </read_first>
  <action>
    In NewspaperLayout.tsx, make these changes. All changes are within the JSX render and the <style jsx global> block. Do NOT modify the component logic, state, forwardRef, useImperativeHandle, export handlers, or type definitions.

    **JSX changes (lines 222-248, masthead + headline sections):**

    1. Masthead title (line 226): Change text content from "Calm Carbon Report" to "The Daily Calm" per D-53 + UI-SPEC masthead copy.

    2. Masthead subtitle: After the masthead date paragraph (`<p className="newspaper-masthead-date">` on line 228), add a conditional subtitle paragraph:
       ```jsx
       {subtitle && (
         <p className="newspaper-masthead-subtitle">{subtitle}</p>
       )}
       ```
       This uses the existing `subtitle` prop. The subtitle text is set by the parent page (edition page passes "Your Carbon Story, in Print" per UI-SPEC Copywriting Contract).

    **Style changes (within <style jsx global>):**

    3. `.newspaper-broadsheet` (line 367-368): Change `font-family: Georgia, "Times New Roman", Times, serif;` to `font-family: var(--font-newspaper-body), 'PT Serif', Georgia, serif;` per D-53 font fallback stack.

    4. `.newspaper-masthead-title` (line 431): Change `font-family: "Times New Roman", Georgia, serif;` to `font-family: var(--font-newspaper-headline), 'Playfair Display', 'Times New Roman', serif;` per D-53 font fallback stack.

    5. `.newspaper-masthead-date` (line 442): Change `font-family: Georgia, "Times New Roman", serif;` to `font-family: var(--font-newspaper-body), 'PT Serif', Georgia, serif;`. Keep font-size 12px (within Body tier 9-12px range).

    6. `.newspaper-headline-title` (line 469): Change `font-family: "Times New Roman", Georgia, serif;` to `font-family: var(--font-newspaper-headline), 'Playfair Display', 'Times New Roman', serif;`.

    7. `.newspaper-headline-subtitle` (line 478): Change `font-family: Georgia, serif;` to `font-family: var(--font-newspaper-body), 'PT Serif', Georgia, serif;`. Keep font-size 16px → reduce to 14px to align with Subhead tier (13-15px per UI-SPEC). Change font-style from italic to normal (subtitle/deck is Regular per Subhead tier).

    8. `.newspaper-footprint-number` (line 495): Change `font-family: "Times New Roman", Georgia, serif;` to `font-family: var(--font-newspaper-headline), 'Playfair Display', 'Times New Roman', serif;`.

    9. `.newspaper-footprint-unit` (line 505): Change `font-family: Georgia, serif;` to `font-family: var(--font-newspaper-body), 'PT Serif', Georgia, serif;`.

    10. `.newspaper-section-label` (line 516): Change `font-family: "Times New Roman", Georgia, serif;` to `font-family: var(--font-newspaper-headline), 'Playfair Display', 'Times New Roman', serif;`. Keep font-weight 700 (Playfair Display only loads weight 700 — matches).

    11. `.newspaper-category-table` (line 537): Change `font-family: Georgia, serif;` to `font-family: var(--font-newspaper-body), 'PT Serif', Georgia, serif;`.

    12. `.newspaper-category-table th` (line 544): Change `font-weight: 600` to `font-weight: 700` (PT Serif loads 400/700 — no 600 weight file per UI-SPEC Declared Font Weights). Change `font-family` implicitly inherited from body — no separate font-family declaration needed here.

    13. `.newspaper-streak-value` (line 584): Change `font-family: "Times New Roman", Georgia, serif;` to `font-family: var(--font-newspaper-headline), 'Playfair Display', 'Times New Roman', serif;`.

    14. `.newspaper-streak-label` (line 591): Change `font-family: Georgia, serif;` to `font-family: var(--font-newspaper-body), 'PT Serif', Georgia, serif;`.

    15. `.newspaper-quote p` (line 614): Change `font-family: Georgia, serif;` to `font-family: var(--font-newspaper-body), 'PT Serif', Georgia, serif;`. Keep font-style: italic (pull quotes use italic per Body tier).

    16. `.newspaper-quote-source` (line 624): Change `font-family: Georgia, serif;` to `font-family: var(--font-newspaper-body), 'PT Serif', Georgia, serif;`. Keep font-style: normal.

    17. `.newspaper-footer-text` (line 648): Change `font-family: Georgia, serif;` to `font-family: var(--font-newspaper-body), 'PT Serif', Georgia, serif;`.

    18. Add new style block for `.newspaper-masthead-subtitle`: font-family: var(--font-newspaper-body), 'PT Serif', Georgia, serif; font-size: 13px; color: #6b6b68; text-align: center; letter-spacing: 0.05em; margin: 0 0 0.05in; font-style: normal.

    19. Ensure the existing BroadsheetExport test line 101 (`screen.getByText("Calm Carbon Report")`) will need updating in Plan 04 — this plan does not modify tests. The changed masthead text is the correct behavior.

    Do NOT change column-count, background color, background gradient, or add any SVG filters — those belong in Plan 02.
  </action>
  <verify>
    <automated>grep -c "The Daily Calm" frontend/src/components/broadsheet/NewspaperLayout.tsx | xargs -I{} sh -c '[ {} -ge 1 ]'</automated>
  </verify>
  <acceptance_criteria>
    - Masthead h1 contains text "The Daily Calm" (not "Calm Carbon Report")
    - Conditional subtitle paragraph with class "newspaper-masthead-subtitle" renders when `subtitle` prop is provided
    - `.newspaper-broadsheet` font-family uses `var(--font-newspaper-body)` with PT Serif fallback stack
    - `.newspaper-masthead-title` font-family uses `var(--font-newspaper-headline)` with Playfair Display fallback stack
    - All font-family declarations in <style jsx global> use the new CSS variables (no remaining `Georgia, "Times New Roman"` or `"Times New Roman", Georgia` for newspaper content)
    - `.newspaper-category-table th` font-weight is 700 (not 600)
    - `.newspaper-headline-subtitle` font-size is 14px (not 16px), font-style is normal (not italic)
    - `grep -c 'Georgia.*serif' frontend/src/components/broadsheet/NewspaperLayout.tsx` shows only fallback declarations within the font stack (e.g., inside `var(...), 'PT Serif', Georgia, serif`)
    - `grep -c '"Times New Roman".*serif' frontend/src/components/broadsheet/NewspaperLayout.tsx` shows only fallback declarations within the font stack
    - Existing BroadsheetExport test that checks for "Calm Carbon Report" will fail — this is expected and will be fixed in Plan 04
  </acceptance_criteria>
  <done>NewspaperLayout masthead reads "The Daily Calm" with conditional subtitle support. All content Text uses Playfair Display for headlines/masthead/footprint numbers and PT Serif for body/captions/footer. Font stack includes CSS variable first, then named fallback, then generic serif.</done>
</task>

</tasks>

<verification>
Run the existing test suite to confirm no unexpected breakage (the masthead text test will fail — expected):
```bash
cd frontend && pnpm test -- --reporter=verbose 2>&1 | tail -30
```
Confirm the app builds with new fonts:
```bash
cd frontend && npx next build 2>&1 | tail -20
```
</verification>

<success_criteria>
- Playfair Display 700 and PT Serif 400/700 load without build errors
- CSS variables --font-newspaper-headline and --font-newspaper-body are set on <html>
- NewspaperLayout renders "The Daily Calm" masthead with Playfair Display
- Body text renders in PT Serif
- No runtime errors from missing font references
</success_criteria>

<output>
Create `.planning/phases/04.1-edition-edits/04.1-01-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces

| Symbol | Kind | File | Description |
|--------|------|------|-------------|
| `Playfair_Display` | import (next/font/google) | `frontend/src/app/layout.tsx` | Font loader for Playfair Display Bold weight 700 |
| `PT_Serif` | import (next/font/google) | `frontend/src/app/layout.tsx` | Font loader for PT Serif weights 400, 700 |
| `playfairDisplay` | const (FontInstance) | `frontend/src/app/layout.tsx` | Configured Playfair Display instance with CSS variable --font-newspaper-headline |
| `ptSerif` | const (FontInstance) | `frontend/src/app/layout.tsx` | Configured PT Serif instance with CSS variable --font-newspaper-body |
| `--font-newspaper-headline` | CSS variable | `frontend/src/app/layout.tsx` | Injected on html element via next/font variable prop |
| `--font-newspaper-body` | CSS variable | `frontend/src/app/layout.tsx` | Injected on html element via next/font variable prop |
| `.newspaper-masthead-subtitle` | CSS class | `frontend/src/components/broadsheet/NewspaperLayout.tsx` | Style for conditional masthead subtitle paragraph |
