---
phase: 01-the-foundation-the-interview-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/database.py
  - backend/app/models/session.py
  - backend/app/api/interview.py
autonomous: true
requirements: [Tech-Spike]
must_haves:
  truths:
    - "Backend can store and retrieve interview sessions"
  artifacts:
    - path: "backend/app/database.py"
      provides: "PostgreSQL connection"
    - path: "backend/app/models/session.py"
      provides: "Session and Message schema"
  key_links:
    - from: "backend/app/api/interview.py"
      to: "backend/app/models/session.py"
      via: "SQLAlchemy"
      pattern: "SessionLocal"
---

<objective>
Tech Spike: Setup PostgreSQL database connection and core SQLAlchemy models for ephemeral session tracking.
Purpose: Provides the data foundation for the AI Coach and The Edition.
Output: Database connection, Session/Message models, and session creation API.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-the-foundation-the-interview-mvp/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB Connection & Models</name>
  <files>backend/app/database.py, backend/app/models/session.py, backend/tests/test_database.py</files>
  <behavior>
    - Test 1: Connects to PostgreSQL using DATABASE_URL.
    - Test 2: Defines Session model with UUID id.
    - Test 3: Defines Message model linked to Session with role (user/ai) and content.
  </behavior>
  <action>
    Implement SQLAlchemy async setup in database.py.
    Create `Session` and `Message` models in models/session.py to track the ephemeral interview history. Ensure UUIDs are used for session tracking (D-13).
  </action>
  <verify>
    <automated>pytest backend/tests/test_database.py</automated>
  </verify>
  <done>Models are defined and can be queried via SQLAlchemy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Session API Endpoint</name>
  <files>backend/app/api/interview.py, backend/tests/test_api_interview.py</files>
  <behavior>
    - Test 1: POST /api/interview/start creates a session and returns the UUID.
  </behavior>
  <action>
    Create a FastAPI POST endpoint `/api/interview/start` that initializes a new Session in the database and returns its `id`.
  </action>
  <verify>
    <automated>pytest backend/tests/test_api_interview.py</automated>
  </verify>
  <done>Endpoint successfully creates and returns a valid session ID.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted session creation requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | interview.py | mitigate | Use secure random UUIDs for session IDs to prevent guessing |
</threat_model>