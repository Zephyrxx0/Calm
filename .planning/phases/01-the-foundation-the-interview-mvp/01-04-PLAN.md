---
phase: 01-the-foundation-the-interview-mvp
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - frontend/src/app/interview/page.tsx
  - frontend/src/components/chat/ChatInterface.tsx
  - frontend/src/app/edition/[sessionId]/page.tsx
autonomous: false
requirements: [Frontend]
must_haves:
  truths:
    - "User can answer via text or quick-answer buttons"
    - "AI output streams with a typewriter effect using @ai-sdk/react"
    - "User sees a personalized broadsheet newspaper output and can export as image"
  artifacts:
    - path: "frontend/src/components/chat/ChatInterface.tsx"
      provides: "Main conversation UI"
    - path: "frontend/src/app/edition/[sessionId]/page.tsx"
      provides: "Edition Page Layout"
  key_links:
    - from: "frontend/src/components/chat/ChatInterface.tsx"
      to: "/api/interview/message"
      via: "fetch API via useChat"
      pattern: "useChat"
    - from: "frontend/src/app/edition/[sessionId]/page.tsx"
      to: "/api/edition"
      via: "fetch API"
      pattern: "fetch.*api/edition"
---

<objective>
Frontend Implementation: Build the unhurried Chat UI utilizing Vercel AI SDK, and the final Edition broadsheet layout with image export.
Purpose: Provide the core user experience for data collection and the concluding typeset reward.
Output: Chat UI with typewriter effect, text/button hybrid input, and the Edition dynamic route.
</objective>

<context>
@.planning/UI-SPEC.md
@.planning/phases/01-the-foundation-the-interview-mvp/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chat Interface UI</name>
  <files>frontend/src/components/chat/ChatInterface.tsx, frontend/src/app/interview/page.tsx, frontend/tests/ChatInterface.test.tsx</files>
  <behavior>
    - Test 1: Renders scrollable list of previous messages (D-07).
    - Test 2: Renders hybrid input (text box + quick range buttons) (D-06).
    - Test 3: Initializes Vercel AI SDK useChat correctly.
  </behavior>
  <action>
    Build the `ChatInterface`. Include typewriter-style input (D-05) and quick-answer buttons (D-06).
    Wire the `/interview` page to start a session on mount (POST `/api/interview/start`), yielding a `session_id`.
    Utilize `@ai-sdk/react` (`useChat`) to consume the Server-Sent Events (SSE) stream from the backend endpoint (POST `/api/interview/{session_id}/message`) (D-08). Note that you need to pass the correct `api` route to `useChat`.
  </action>
  <verify>
    <automated>npm run test --prefix frontend</automated>
  </verify>
  <done>Chat interface interacts with API using useChat and displays streaming text in monochrome aesthetic.</done>
</task>

<task type="auto">
  <name>Task 2: Edition Page Layout</name>
  <files>frontend/src/app/edition/[sessionId]/page.tsx</files>
  <action>
    Build the page for dynamic route `/edition/[sessionId]`. Fetch data from GET `/api/edition/{session_id}`.
    Implement the multi-column broadsheet layout (D-15).
    Include a bold personalized headline, footprint metric, category breakdown, and pull quotes.
    Add `@media print` CSS so it exports nicely to PDF, and add a capability to export the layout as an image using `html-to-image` (D-14).
  </action>
  <verify>
    <automated>npm run lint --prefix frontend</automated>
  </verify>
  <done>Edition page dynamically fetches and renders strict broadsheet aesthetic with PDF and image export capabilities.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The End-to-End Interview and Edition Output.</what-built>
  <how-to-verify>
    1. Start the frontend and backend servers.
    2. Navigate to localhost:3000 and click "Begin Your Interview".
    3. Complete a brief conversation with the AI coach.
    4. Verify the flow routes you to the Edition page at the end.
    5. Verify the Edition layout looks like a newspaper, prints well to PDF (Cmd+P), and can be exported as an image.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/functional issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client | XSS via chat messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Elevation | ChatInterface.tsx | mitigate | React escapes strings, ensure no dangerouslySetInnerHTML is used for message content |
| T-01-06 | Info Disclosure | Edition Page | mitigate | Session IDs are UUIDv4. Data is ephemeral. |
</threat_model>