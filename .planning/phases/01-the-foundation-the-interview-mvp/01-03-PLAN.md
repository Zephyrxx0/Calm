---
phase: 01-the-foundation-the-interview-mvp
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - backend/app/services/carbon_model.py
  - backend/app/services/ai_coach.py
  - backend/app/api/interview.py
  - backend/app/api/edition.py
autonomous: true
user_setup:
  - service: "Google Cloud AI"
    why: "Gemini integration for the AI Coach"
    env_vars:
      - name: "GEMINI_API_KEY"
        source: "Google AI Studio"
requirements: [AI-Coach, Carbon-Model]
must_haves:
  truths:
    - "AI responds with contextually relevant questions across 5 categories"
    - "System calculates footprint using hybrid logic"
    - "Backend streams responses using Vercel AI SDK Data Stream Protocol"
  artifacts:
    - path: "backend/app/services/ai_coach.py"
      provides: "Gemini State Machine"
    - path: "backend/app/services/carbon_model.py"
      provides: "Calculation Logic"
  key_links:
    - from: "backend/app/api/interview.py"
      to: "backend/app/services/ai_coach.py"
      via: "Function call"
      pattern: "ai_coach\\.generate_response"
---

<objective>
AI Coach Backend: Develop the AI Coach service using Gemini, Carbon Model, and Chat/Edition endpoints.
Purpose: Drives the core conversation logic, ensures 5-category coverage, streams AI responses, and computes the final footprint.
Output: State machine for question sequencing, AI integration, footprint calculator, and API routes compliant with Vercel AI SDK protocol.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-the-foundation-the-interview-mvp/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carbon Calculation Model</name>
  <files>backend/app/services/carbon_model.py, backend/tests/test_carbon_model.py</files>
  <behavior>
    - Test 1: Calculates footprint for given JSON payload using hardcoded coefficients.
    - Test 2: Returns breakdown by 5 categories (Commute, Travel, Home, Diet, Shopping).
  </behavior>
  <action>
    Implement simple, hardcoded category-level average coefficients for carbon calculation (D-10).
    Take structured data (e.g., { "diet": "vegan", "flights": 2 }) and return total kg CO2e and breakdown.
    Validate the selected coefficient choices against standard carbon factor databases during implementation and document sources in comments (D-11).
  </action>
  <verify>
    <automated>pytest backend/tests/test_carbon_model.py</automated>
  </verify>
  <done>Calculation logic computes reliable estimates based on structured inputs and validated coefficients.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gemini State Machine</name>
  <files>backend/app/services/ai_coach.py, backend/tests/test_ai_coach.py</files>
  <behavior>
    - Test 1: Generates initial greeting and first question.
    - Test 2: Parses user intent and transitions across 5 categories.
    - Test 3: Returns JSON with expected format (text, is_complete, extracted_data).
  </behavior>
  <action>
    Integrate `google-generativeai`.
    Implement the state machine (D-01) controlling the 5 categories (D-03) and 20-25 question cap (D-04).
    Prompt the AI to act as a calm journalist. Use Structured Outputs or prompt formatting to get both the conversational reply and any extracted data payload (D-09).
  </action>
  <verify>
    <automated>pytest backend/tests/test_ai_coach.py</automated>
  </verify>
  <done>AI correctly follows categories and extracts data without hallucinations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: API Endpoints (Chat & Edition)</name>
  <files>backend/app/api/interview.py, backend/app/api/edition.py, backend/tests/test_api_endpoints.py</files>
  <behavior>
    - Test 1: POST /message receives user text, updates DB, calls ai_coach, and returns an SSE stream.
    - Test 2: GET /edition/{session_id} returns footprint, categories, and quotes.
    - Test 3: SSE stream output from POST /message uses the Vercel AI SDK `0:"..."` protocol format.
  </behavior>
  <action>
    Create POST `/api/interview/{session_id}/message` to handle message roundtrips. This endpoint must stream AI responses formatted as Server-Sent Events (SSE) compliant with the Vercel AI SDK Data Stream Protocol (e.g., using `0:"text"` parts for text chunks).
    Create GET `/api/edition/{session_id}` to retrieve session history, compute footprint via `carbon_model`, extract 1-2 pull quotes, and return Edition data for frontend.
  </action>
  <verify>
    <automated>pytest backend/tests/test_api_endpoints.py</automated>
  </verify>
  <done>APIs successfully process chat logic, stream SSE according to Vercel AI SDK protocol, and serve edition data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Gemini | LLM Prompt Injection via user messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | ai_coach.py | mitigate | Validate user input length and use strict system instructions to ignore prompt overrides |
| T-01-04 | Info Disclosure | carbon_model.py | accept | Calculations are generic; no PII involved |
</threat_model>