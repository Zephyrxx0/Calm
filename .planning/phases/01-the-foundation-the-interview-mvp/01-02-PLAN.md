---
phase: 01-the-foundation-the-interview-mvp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/globals.css
  - frontend/src/app/layout.tsx
  - frontend/src/app/page.tsx
autonomous: true
requirements: [Design-System]
must_haves:
  truths:
    - "User sees a vintage broadsheet landing page"
    - "Call to action navigates to interview"
  artifacts:
    - path: "frontend/src/app/globals.css"
      provides: "Monochrome color scheme"
    - path: "frontend/src/app/layout.tsx"
      provides: "Broadsheet typography"
    - path: "frontend/src/app/page.tsx"
      provides: "Landing page UI"
  key_links:
    - from: "frontend/src/app/page.tsx"
      to: "/interview"
      via: "Next Link"
      pattern: "href=[\"']/interview[\"']"
---

<objective>
Design System: Implement the "Vintage Broadsheet" typography, colors, and the Landing Page.
Purpose: Establish the visual identity (monochrome, classic typography) and the entry point for the user.
Output: Global CSS variables, font configuration, and the landing page component.
</objective>

<context>
@.planning/PROJECT.md
@.planning/UI-SPEC.md
@.planning/phases/01-the-foundation-the-interview-mvp/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Broadsheet Typography & Colors</name>
  <files>frontend/src/app/globals.css, frontend/src/app/layout.tsx</files>
  <action>
    Configure standard Next.js fonts to use a classic serif (e.g., Cormorant Garamond or similar Google Font) for primary text and Geist Mono for secondary text.
    In globals.css, setup Tailwind v4 variables for a strict monochrome palette (Paper: #FDFCF7, Ink: #1A1A1A).
    Ensure shadcn components (if any) use this palette. Remove all default gradients/colors per UI-SPEC.
  </action>
  <verify>
    <automated>npm run lint --prefix frontend</automated>
  </verify>
  <done>Fonts load correctly and CSS variables reflect the monochrome palette.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Landing Page UI</name>
  <files>frontend/src/app/page.tsx, frontend/tests/page.test.tsx</files>
  <behavior>
    - Test 1: Renders the project title and a "Begin Your Interview" CTA link.
  </behavior>
  <action>
    Implement the landing page with a minimalist, newspaper front-page aesthetic.
    Include a clear "Begin Your Interview" link/button navigating to `/interview`.
    Ensure styles match UI-SPEC (e.g., outline buttons, no solid backgrounds).
  </action>
  <verify>
    <automated>npm run test --prefix frontend</automated>
  </verify>
  <done>Landing page visually matches vintage broadsheet and routes to interview.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client | UI rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Spoofing | UI | accept | Static frontend assets, low risk |
</threat_model>