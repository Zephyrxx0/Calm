---
phase: 02-the-ledger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["backend/app/models/session.py", "backend/requirements.txt", "backend/app/services/scanner.py", "backend/tests/test_database.py", "backend/tests/test_scanner.py"]
autonomous: true
requirements: ["OCR-Integration", "Profile-Update"]
must_haves:
  truths:
    - "A database query for a session returns its newly associated ledger entries."
    - "Providing a receipt image to the scanner service returns extracted carbon data (description, category, impact)."
    - "The AI provider's file list shows no remaining uploaded files after the scanner service completes."
  artifacts:
    - path: "backend/app/models/session.py"
      provides: "LedgerEntry SQLAlchemy model"
      contains: "class LedgerEntry(Base):"
    - path: "backend/app/services/scanner.py"
      provides: "File scanning service"
      exports: ["scan_receipt_or_bill"]
  key_links:
    - from: "backend/app/services/scanner.py"
      to: "google-genai"
      via: "File API upload and generate_content"
      pattern: "client.aio.files.upload"
---

<objective>
Build the data layer and AI service for the Ledger.

Purpose: Provide ephemeral database storage for ledger entries and integrate the Gemini File API to extract carbon data from receipts and utility bills.
Output: `LedgerEntry` database model and a `scanner` service for OCR data extraction.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-the-ledger/02-CONTEXT.md
@.planning/phases/02-the-ledger/02-RESEARCH.md
@backend/app/models/session.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LedgerEntry Database Model</name>
  <files>backend/app/models/session.py, backend/tests/test_database.py</files>
  <action>
    Append a `LedgerEntry` model to the existing `session.py`.
    It must include: `id` (int primary key), `session_id` (ForeignKey to sessions.id, nullable=False), `description` (String), `category` (String), `carbon_impact` (Float), and `created_at` (DateTime, default=func.now()).
    Add a `ledger_entries` relationship to the existing `Session` model (cascade="all, delete-orphan") per D-17.
    Write a test in `backend/tests/test_database.py` verifying the creation of a LedgerEntry linked to a Session.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_database.py</automated>
  </verify>
  <done>Model is defined with foreign key to Session, and test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Gemini Scanner Service</name>
  <files>backend/requirements.txt, backend/app/services/scanner.py, backend/tests/test_scanner.py</files>
  <action>
    1. Add `python-multipart>=0.0.9` to `requirements.txt`.
    2. Create `scanner.py`. Implement an async function `scan_receipt_or_bill(file_path: str, mime_type: str) -> dict`.
    3. Use the `google-genai` SDK (`client.aio.files.upload`, `client.aio.models.generate_content`). If PDF, you must poll `client.aio.files.get` until `state == "ACTIVE"` before generating.
    4. Prompt the model to extract `description`, `category` (e.g., Energy, Transport, Food, Shopping), and an estimated `carbon_impact` (in kg CO2e) as JSON.
    5. CRITICAL (per D-17): Ensure a `finally` block calls `client.aio.files.delete` to delete the uploaded file from Google immediately after extraction.
    6. Write a test (using mocks for the SDK) in `backend/tests/test_scanner.py` to ensure the extraction logic and the `finally` cleanup block work correctly.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_scanner.py</automated>
  </verify>
  <done>Service can extract data, always deletes the remote file, and has passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File Upload | Client submits arbitrary files to backend |
| AI API | Extracted data from AI is parsed into DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | python-multipart | mitigate | slopcheck dependency addition |
| T-02-02 | Info Disclosure | scanner.py | mitigate | Enforce remote file deletion in finally block |
| T-02-03 | Spoofing | AI Output | mitigate | Validate AI JSON output before inserting to DB |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `python-multipart` is present in requirements.txt.
- `LedgerEntry` maps to `Session`.
- Scanner service enforces file deletion.
</verification>

<success_criteria>
Backend infrastructure for Ledger data ingestion is complete.
</success_criteria>

<output>
Create `.planning/phases/02-the-ledger/02-01-SUMMARY.md` when done
</output>
