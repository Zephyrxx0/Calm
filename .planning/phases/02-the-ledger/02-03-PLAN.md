---
phase: 02-the-ledger
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: ["frontend/src/app/ledger/[sessionId]/page.tsx", "frontend/src/components/ledger/LedgerView.tsx", "frontend/src/components/ledger/FileUpload.tsx", "frontend/src/app/interview/page.tsx", "frontend/tests/Ledger.test.tsx"]
autonomous: true
requirements: ["Ledger-View", "OCR-Integration", "Profile-Update"]
must_haves:
  truths:
    - "User can view a visually calm, organic list of their ledger entries."
    - "User can upload receipts (images) or bills (PDFs) from the Ledger view."
    - "The Ledger dynamically displays both the newly calculated total carbon footprint and the category breakdown."
    - "User can navigate back and forth between the Interview and the Ledger."
  artifacts:
    - path: "frontend/src/app/ledger/[sessionId]/page.tsx"
      provides: "Ledger route"
      contains: "export default function LedgerPage"
    - path: "frontend/src/components/ledger/LedgerView.tsx"
      provides: "Organic UI for entries and total"
      contains: "function LedgerView"
    - path: "frontend/src/components/ledger/FileUpload.tsx"
      provides: "File upload component"
      exports: ["FileUpload"]
  key_links:
    - from: "frontend/src/components/ledger/FileUpload.tsx"
      to: "backend/api/ledger/upload"
      via: "fetch POST"
      pattern: "fetch(.*upload"
    - from: "frontend/src/app/ledger/[sessionId]/page.tsx"
      to: "backend/api/ledger"
      via: "fetch GET"
      pattern: "fetch(.*api/ledger"
---

<objective>
Build the Ledger UI and connect it to the data ingestion backend.

Purpose: Provide an organic, garden-themed view for users to upload receipts and bills, displaying their dynamic carbon footprint.
Output: Ledger Next.js page, organic UI components, and navigation integration.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-the-ledger/02-CONTEXT.md
@.planning/phases/02-the-ledger/02-RESEARCH.md
@frontend/src/components/OrganicDoodles.tsx
@frontend/src/app/interview/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Organic Ledger Components</name>
  <files>frontend/src/components/ledger/LedgerView.tsx, frontend/src/components/ledger/FileUpload.tsx, frontend/tests/Ledger.test.tsx</files>
  <action>
    1. Create `FileUpload.tsx`: An upload zone allowing JPG, PNG, and PDF files. Send files to `/api/ledger/{sessionId}/upload` via `multipart/form-data`.
    2. Create `LedgerView.tsx`: A list component rendering entries. Use `OrganicDoodles.tsx` elements (like `DoodleLeaf` or `DoodlePebbles`) as dividers instead of harsh lines, adhering to the "Tending a Garden" metaphor (D-21).
    3. Display BOTH the session's dynamically updated total carbon impact AND the updated category breakdown alongside the entries (D-22).
    4. Write tests in `frontend/tests/Ledger.test.tsx` verifying that `LedgerView` correctly renders the total and the category breakdown, and that `FileUpload` exposes the upload interface.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/Ledger.test.tsx</automated>
  </verify>
  <done>UI components are styled organically, handle file uploads, display total+breakdown, and pass tests.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble Ledger Page and Navigation</name>
  <files>frontend/src/app/ledger/[sessionId]/page.tsx, frontend/src/app/interview/page.tsx</files>
  <action>
    1. Create `ledger/[sessionId]/page.tsx` integrating `LedgerView` and `FileUpload`. Use React state/hooks to fetch initial entries, updated total, and category breakdown from `GET /api/ledger/{sessionId}` and maintain their dynamic state (D-22).
    2. In `frontend/src/app/ledger/[sessionId]/page.tsx`, include a "Back to Interview" navigation link returning the user to `/interview` or the relevant final summary page.
    3. In `frontend/src/app/interview/page.tsx`, update the `<header>` to include a "Refine in Ledger" link pointing to `/ledger/{sessionId}` (D-23).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Ledger route is accessible, fully connected to the interview session with bidirectional navigation, and typechecks cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Browser | Rendering AI-extracted descriptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Cross-Site Scripting | LedgerView | mitigate | Ensure React safely escapes `description` and `category` fields when rendering |
</threat_model>

<verification>
- Uploading a file updates the Ledger list and total immediately.
- The UI follows the earthy, calm design language without harsh grids.
- Navigation flows seamlessly from Interview to Ledger.
</verification>

<success_criteria>
The Ledger feature is fully implemented and visually integrated with the Calm application.
</success_criteria>

<output>
Create `.planning/phases/02-the-ledger/02-03-SUMMARY.md` when done
</output>
